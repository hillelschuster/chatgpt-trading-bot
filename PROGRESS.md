# Progress

- 2026-07-22T07:40:00Z | Thesis: liquid hourly funding dislocations are measurable and may become tradable when combined with subsequent price behavior and realistic costs. | Changed: initialized the repository and added a zero-dependency Hyperliquid perp scout ranking funding extremes after open-interest and volume filters. | Evidence: official Hyperliquid docs expose `metaAndAssetCtxs`, hourly funding, public liquidity data, and base perp fees of 0.045% taker / 0.015% maker. | Verification: fixture-based unit test passed locally; live API execution was not available in the coding runtime. | Next: persist hourly snapshots and test whether extreme funding predicts cost-adjusted forward returns.
- 2026-07-22T08:39:00Z | Thesis: the first useful evidence is a clean hourly panel linking funding extremes to later mark returns. | Changed: the scout now appends all qualified assets, marks, funding, OI, volume, and capture time to compact JSONL on every run; added persistence coverage. | Evidence: Hyperliquid funding settles hourly and official docs explicitly recommend recording additional historical datasets through the API. | Verification: JSONL append/read test reproduced locally; GitHub files and commits verified. | Next: add a chronological evaluator that joins snapshots and reports 1h/4h cost-adjusted forward returns by funding bucket.
- 2026-07-22T09:40:00Z | Thesis: funding extremes are worth pursuing only if a chronological fade survives funding receipts and realistic round-trip costs. | Changed: added a reusable 1h/4h/8h/24h evaluator that pairs snapshots within a strict time tolerance, creates explicit long/short observations, subtracts configurable costs, reports win rate/mean/median/profit factor/by-side metrics, and optionally writes a trade ledger; added focused tests and CLI documentation. | Evidence: official Hyperliquid docs confirm hourly funding, public candle/market endpoints, and a base perp fee schedule; evaluator assumptions are exposed as CLI parameters rather than hidden. | Verification: 3 evaluator tests passed locally; all four changed repository files and final commits were verified through GitHub. | Next: bootstrap enough historical funding and candle data from official Hyperliquid endpoints to run the evaluator across multiple regimes immediately instead of waiting for hourly accumulation.
- 2026-07-22T10:38:00Z | Thesis: the funding-fade idea should now be judged on immediate multi-month real history rather than waiting for live snapshots to accumulate. | Changed: added a zero-dependency historical bootstrapper that paginates official funding history, chunks 1h candles within the documented 5,000-candle limit, aligns funding events to hourly closes, writes evaluator-compatible JSONL, and exposes coin/day/output CLI controls; added focused pagination/chunking/join/persistence tests and updated usage docs. | Evidence: official Hyperliquid API documentation specifies `fundingHistory`, `candleSnapshot`, supported 1h candles, and the 5,000-candle retrieval limit. | Verification: 3 new bootstrap tests passed locally; repository files and final GitHub commits were re-read and verified. | Next: run the bootstrapper against live public endpoints, evaluate several thresholds/horizons across BTC/ETH/SOL, and commit a compact reproducible result artifact that either supports or rejects the funding-fade thesis.
- 2026-07-22T11:37:00Z | Thesis: the funding-fade hypothesis must be judged without crediting the already-settled funding event that creates the signal and without selecting parameters on future data. | Changed: corrected held-funding accounting, added uncertainty/drawdown metrics, implemented chronological 70/30 parameter selection and out-of-sample verdicts, expanded tests, and added a GitHub workflow that tests, downloads 180 days of BTC/ETH/SOL history, runs the study, and commits the compact report. | Evidence: a trade entered after observing an hourly funding print cannot receive that same print; selecting thresholds and horizons on the full sample would leak test-period information. | Verification: 5 focused tests passed locally; all source, test, workflow, and documentation writes were verified on `main`; the first live workflow result was not yet available at diary update time. | Next: inspect `reports/funding_fade.json`, reject the thesis if out-of-sample lower confidence remains non-positive, otherwise add overlap-aware portfolio simulation and capacity checks.
- 2026-07-22T12:37:00Z | Thesis: even a positive trade-level edge is not deployable until overlapping signals, per-coin concentration, finite capital, and position-size capacity are modeled. | Changed: added an overlap-aware portfolio simulator with explicit portfolio/per-coin slot limits, equity-linked sizing, notional caps, compounded realized P&L, rejection diagnostics, drawdown/uncertainty metrics, JSONL ledger output, CLI usage, and four focused tests. | Evidence: hourly multi-asset signals can overlap, so summing independent trade returns materially overstates deployable performance and capital usage. | Verification: 4 portfolio tests passed locally; source, tests, README, diary, GitHub writes, and final commit were verified. | Unresolved: the live funding report is still absent, so no edge or profitability claim is made. | Next: integrate the selected out-of-sample configuration with trade export and portfolio simulation in the automated live workflow, then inspect the committed real-data result.
- 2026-07-22T13:38:00Z | Thesis: deployability must be evaluated from the exact untouched trades selected by the research process, with capital changing only when positions actually exit. | Changed: integrated out-of-sample trade export and portfolio simulation into `research.py`; fixed the simulator's premature entry-time P&L realization; added exit-time settlement, reserved-capital sizing, utilization diagnostics, stronger tests, and a workflow that commits the study, raw OOS trades, and accepted ledger. | Evidence: realizing future P&L at entry leaks information into later overlapping position sizes and understates path risk. | Verification: all seven changed files and commits were re-read through GitHub; focused test coverage was updated, while the triggered GitHub Actions execution and live 180-day artifacts were not yet available for inspection in this run. | Unresolved: no profitability claim until the workflow publishes real-data evidence. | Next: inspect the generated report and ledger; reject funding fade if OOS confidence or deployable portfolio return fails, otherwise add walk-forward regime stability and execution-cost sensitivity.
- 2026-07-22T14:39:00Z | Thesis: the first live train/test result rejects deployment, so the strategy must survive repeated untouched periods rather than receive more single-split tuning. | Changed: added an anchored walk-forward validator that reselects parameters using past-only data, aggregates untouched trades, simulates their deployable portfolio, measures parameter/fold/per-coin stability, and stress-tests 3–12 bps costs; integrated it into CI with durable reports, trades, and ledgers. | Evidence: the 180-day report selected an 8h/0.5bps long-only setup, but OOS mean LCB95 was -0.4305%, deployable return was -0.7093%, and the verdict was `REJECT_OR_REWORK`. | Verification: source, tests, workflow, README, and final GitHub writes were re-read; local execution was unavailable because the runtime could not resolve GitHub, and the new workflow result is pending. | Next: inspect `reports/walkforward.json`; if rejected, retire funding fade and build a cost-aware cross-sectional momentum/reversal benchmark from the same data before choosing the next bot thesis.
- 2026-07-22T15:39:00Z | Thesis: after funding fade failed its first live OOS gate, the fastest rational next step is a directly comparable price-only benchmark rather than further tuning the rejected signal. | Changed: added a complete cross-sectional momentum/reversal study over 1/4/8/24-hour lookbacks and horizons, 3–12 bps costs, train-only selection, untouched OOS trade export, finite-capital two-sided portfolio simulation, tests, CLI docs, and CI publication. | Evidence: this benchmark tests whether relative return persistence or reversal among the same liquid assets offers a cleaner mechanism than funding extremes without requiring new paid data. | Verification: 3 focused tests passed locally; all five repository files were created or updated and GitHub writes were verified. The live 180-day comparative report is pending. | Next: inspect `reports/xsection.json`; retain only a thesis with positive OOS confidence and deployable return, otherwise broaden the universe before pursuing execution.
- 2026-07-22T16:39:00Z | Thesis: a three-asset ranking is not a credible cross-sectional test; the benchmark needs materially broader liquid-perp breadth before its verdict is useful. | Changed: added automatic top-liquidity universe selection from official Hyperliquid metadata, API retries, sparse-hour filtering, panel quality/universe artifacts, breadth-aware cross-sectional signals requiring six common assets, expanded tests, and a 12-asset CI study. | Evidence: ranking one winner and loser from only BTC/ETH/SOL makes results highly idiosyncratic and unstable; current liquidity metadata provides a free practical exploratory universe. | Verification: changed Python files were syntax-checked before writing; all seven repository writes and their returned commit SHAs were verified. Full GitHub Actions real-data execution is pending. | Unresolved: selecting today’s liquid universe creates survivorship bias, explicitly documented; positive evidence remains exploratory until historical membership or archived universe snapshots are used. | Next: inspect the broad-universe report, then add repeated archived universe snapshots or a survivorship-resistant fixed historical universe before any execution work.
- 2026-07-22T17:45:00Z | Audit verdict: no profitable edge is established; the previous evidence is invalid because candle closes were stored at candle-open timestamps, creating one-hour lookahead. | Changed: corrected the historical price timestamp, fixed rather than optimized a conservative 12 bps cost, excluded ambiguous funding boundaries, added funding and pair-level/non-overlap controls to cross-sectional tests, improved API pacing, clarified realized-only drawdown, separated fast CI from daily/manual research, and invalidated stale reports. | Evidence: the only durable live report already rejected funding fade with an OOS LCB95 of -0.4305% and a deployable return of -0.7093%; later reports never materialized. | Verification: 21 focused tests and Python compilation passed locally. | Next: run one corrected 180-day research artifact and review it before adding another strategy or any execution code.
