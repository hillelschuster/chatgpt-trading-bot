# Progress

## Retired historical research

- 2026-07-22T07:40:00Z–16:39:00Z | Built the initial Hyperliquid funding scout, persistence, historical bootstrap, chronological evaluator, finite-capital simulator, anchored walk-forward validation, and cross-sectional momentum/reversal benchmark. Early workflow evidence was later invalidated after discovering candle closes stored at candle-open timestamps.
- 2026-07-22T17:45:00Z | Corrected the one-hour lookahead, fixed rather than optimized costs, tightened funding boundaries, added pair-level/non-overlap controls, and separated fast CI from real-data research.
- 2026-07-22T19:10:00Z | Corrected fixed-universe artifact `research-29949365810` used 4,320 complete hourly records for eleven assets. Funding fade walk-forward mean/LCB95 were -0.1452%/-0.3107% with -17.74% portfolio return; cross-sectional OOS mean/LCB95 were -0.1315%/-0.1791% with -81.99% portfolio return and remained negative at 9–18 bps. Both strategies were retired.

## Prospective Hyperliquid–OKX experiment

- 2026-07-22T23:26:00Z | Verified Hyperliquid–OKX as the public runner-accessible pair for a prospective BTC/ETH delta-neutral funding/basis experiment; Binance and Bybit were rejected after reproducible runner restrictions. Added the frozen experiment specification and live collector probe.
- 2026-07-23T00:18:00Z | Added schema-v4 five-minute collection, future-effective Hyperliquid boundary normalization, resumable `(cadence_slot_ms, coin)` persistence, and deterministic continuity/duplicate/gap validation.
- 2026-07-23T01:09:00Z | Added leakage-safe event windows with pre-boundary signal freezing, delayed adverse entry/exit books, coordination checks, and explicit pending/rejected states.
- 2026-07-23T02:12:00Z–03:14:00Z | Added exact Hyperliquid/OKX realized-funding joins and resumable monotonic settlement enrichment that preserves partial observations and transient failures.
- 2026-07-23T04:16:00Z | Added fail-closed append-only chain verification across snapshots, events, settlements, and state transitions.
- 2026-07-23T05:00:00Z–06:00:00Z | Added frozen two-leg P&L accounting with 15.5 bps base and 20 bps stress costs, one-leg failure reserves, exact settlement requirements, and a hard 200-complete-event no-inference gate.
- 2026-07-23T07:00:00Z | Added fixed prospective validation: 140 complete development events, 60 complete holdout events, deterministic moving-block bootstrap, finite-capital base/stress portfolios, concentration and failed-attempt gates.
- 2026-07-23T08:00:00Z–10:00:00Z | Bound evidence to an immutable freeze manifest, included workflow orchestration and validation logic in the frozen contract, added safe pre-evidence freeze-v2 migration, rejected mixed-manifest evidence, and extended append-only verification to the manifest and scored P&L rows.
- 2026-07-23T11:09:00Z | Thesis: same-boundary BTC/ETH attempts must be one synchronized portfolio period, not sequential pseudo-trades. | Changed: grouped attempts by funding boundary; sized every simultaneous leg from identical pre-period equity; compounded only after aggregate period P&L; moved block-bootstrap inference to capital-weighted period returns; added an impossible-allocation fail-close and order-independence tests; documented the contract; repaired the malformed diary tail into this compact milestone history. | Evidence: sequential same-time compounding was row-order dependent and treated correlated boundary observations as separate capital updates. | Verification: 9 focused validation tests and Python compilation passed locally; repository source, tests, and documentation were written and re-read. | Unresolved: the next scheduled artifact must migrate the pre-evidence freeze to the new validation hash and confirm a valid restored chain. | Next: inspect the first post-change persistent artifact for safe freeze migration, zero chain errors, and advancing complete event/P&L counts.
- 2026-07-23T12:10:00Z | Thesis: synchronized BTC/ETH legs are one statistical observation, so attempt counts cannot satisfy the promotion sample; elapsed time and the frozen specification must also be enforced in code. | Changed: partitioned and gated on 140 development plus 60 holdout complete funding periods rather than legs, added separate attempt/period diagnostics, enforced the frozen 56-day minimum, aligned deterministic concentration and failed-attempt thresholds to 70% and below 5%, and updated the experiment contract and tests. | Evidence: 200 simultaneous attempts represented only 100 correlated periods and could previously reach the nominal sample gate in roughly eight days, contradicting the frozen eight-week independent-event design. | Verification: 12 focused validation tests and Python compilation passed locally, including sample-inflation, elapsed-time, failure-rate, concentration, manifest, and order-independence cases; changed files and returned GitHub SHAs were verified. | Unresolved: the next scheduled artifact must perform the permitted pre-evidence freeze migration to the new validation/specification hashes and report zero chain mutations. | Next: inspect the first post-change persistent artifact for safe freeze migration, valid restoration, and correct complete-attempt versus complete-period counts.
- 2026-07-23T13:28:00Z | Thesis: a promotion report cannot be trusted if it is calculated before the restored artifact chain is proven append-only. | Changed: moved chain verification ahead of validation, bound the validation verdict to the same-run chain report, made missing or invalid chain evidence fail closed as `INVALID`, suppressed holdout metrics and ledgers on integrity failure, and added focused regression coverage. | Evidence: the workflow previously produced validation before chain verification, so a standalone `PASS` report could exist even when the later chain audit failed. | Verification: 13 focused validation tests, Python compilation, and workflow YAML parsing passed locally; source, tests, workflow, README, and diary were written and verified on `main`. | Unresolved: the next scheduled artifact must demonstrate a valid same-run chain report feeding validation after the freeze migration. | Next: inspect the next persistent artifact for `artifact_chain.valid=true`, zero errors, a valid freeze upgrade, and nondecreasing evidence counts.
- 2026-07-23T14:15:00Z | Thesis: profitable surviving events are not credible if collector outages selectively remove adverse periods. | Changed: added deterministic post-freeze five-minute cadence coverage, synchronized BTC/ETH slot coverage, duplicate detection, observed funding-opportunity accounting, and an authoritative promotion report requiring both profitability validation and 56-day observation coverage to pass; integrated tests, compilation, workflow artifacts, documentation, and fixed zero-timestamp handling found during verification. | Evidence: the prior gate enforced immutable surviving evidence but did not prevent missing-data selection bias. | Verification: four focused coverage/promotion tests and Python compilation passed locally; changed repository files and returned GitHub SHAs were verified. | Unresolved: the next scheduled artifact must execute the new coverage and promotion stages against the restored prospective series. | Next: inspect `crossvenue_coverage.json` and `crossvenue_promotion.json` for correct collecting status, no duplicates, and measured slot/event coverage.
- 2026-07-23T15:10:00Z | Thesis: observation-coverage and authoritative promotion code are part of the strategy contract because changing either after seeing evidence could manufacture a pass without changing P&L logic. | Changed: added `crossvenue_coverage.py` and `crossvenue_promote.py` to the immutable freeze manifest, added regression tests proving both are frozen and that newly added contract files force a safe pre-evidence re-freeze, and documented the expanded contract. | Evidence: the prior manifest protected collection, accounting, chain, and validation code but omitted the newly introduced missing-data and final-verdict gates. | Verification: six focused freeze tests and Python compilation passed locally; repository writes and returned commit SHAs were verified. | Unresolved: the next scheduled run must safely upgrade the manifest before any complete post-freeze P&L evidence and show both new hashes in `crossvenue_freeze.json`. | Next: inspect the next persistent artifact for the safe upgrade, unchanged cutoff monotonicity, valid chain, and frozen coverage/promotion hashes.
- 2026-07-23T16:07:00Z | Thesis: any code change made after inspecting raw prospective observations must exclude those observations, even when no exact P&L row exists yet. | Changed: safe re-freezes now compute the evidence cutoff from raw snapshots, event windows, settlements, and P&L rows; added per-stream watermarks, raw-snapshot timestamp handling, workflow wiring for all four streams, regression tests, and corrected a stale freeze test to the manifest-bound validation API. | Evidence: the prior workflow passed only `crossvenue_pnl_events.jsonl`, allowing already-seen raw market observations to remain promotion-eligible after research logic changed. | Verification: three focused new freeze tests and Python compilation passed locally; repository source, tests, workflow, README, and diary were written. | Unresolved: the next persistent run must safely migrate the freeze and show the cutoff equal to the maximum restored stream watermark with a valid append-only chain. | Next: inspect `crossvenue_freeze.json` watermarks and confirm every pre-migration observation is excluded before further contract changes.
- 2026-07-23T17:10:00Z | Thesis: once the experiment contract is frozen, hourly operational work must observe evidence health without changing frozen logic or advancing the prospective cutoff. | Changed: added a standalone health summarizer and separate hourly workflow that download the latest immutable artifact and report staleness, append-only integrity, post-freeze counts, unique completed funding periods, coverage, and remaining promotion requirements; the monitor is intentionally excluded from the freeze manifest. | Evidence: repeated edits to frozen modules would continually re-freeze and discard observed data, preventing the 56-day prospective gate from ever completing. | Verification: four focused health tests and Python compilation passed locally; repository source, tests, workflow, README, and diary were written. | Unresolved: the first health workflow must inspect a persistent artifact after the all-stream cutoff migration. | Next: inspect `crossvenue_health.json`; if healthy, stop modifying frozen modules and allow evidence accumulation, fixing only demonstrated data or integrity failures.
- 2026-07-23T18:10:00Z | Thesis: an operational health monitor must fail closed when its source artifact is partial; otherwise a failed collector can masquerade as normal evidence accumulation. | Changed: required the complete freeze/snapshot/event/settlement/P&L data set and chain/coverage/validation/promotion reports, made missing chain evidence explicitly invalid, added impossible complete-P&L-versus-settlement consistency checks, exposed exact missing files, expanded regression tests, and documented the fail-closed contract without touching frozen experiment files or advancing the evidence cutoff. | Evidence: the first health implementation treated absent chain and downstream reports as empty optional inputs and could return `ACCUMULATING_*`. | Verification: seven focused health tests and Python compilation passed locally; source, tests, README, diary, and final GitHub writes were re-read. | Unresolved: the first scheduled hourly health artifact must execute against the latest persistent series. | Next: inspect `crossvenue_health.json`; if valid and current, make no frozen-contract changes and continue prospective accumulation.
- 2026-07-23T19:06:00Z | Thesis: prospective evidence must resume from the last completed successful workflow, not merely the newest uploaded artifact. | Changed: added a paginated GitHub artifact selector that skips failed, cancelled, in-progress, expired, and pull-request artifacts; integrated it into both five-minute series restoration and hourly health monitoring; added focused selection tests and documented last-known-good recovery. | Evidence: the collector uploads artifacts even when later steps fail, so timestamp-only restoration could replace a valid evidence chain with a partial failed-run artifact and make a transient outage destructive. | Verification: four focused artifact-selection tests and Python compilation passed locally; workflow integration, source, tests, documentation, diary, and returned GitHub commits were verified. | Unresolved: the next scheduled collector and health runs must prove token-authorized API selection and ZIP restoration against live Actions artifacts. | Next: inspect the first live runs for a selected successful artifact ID, valid chain restoration, and non-stale health output.
- 2026-07-23T20:12:00Z | Thesis: a successful artifact is not trustworthy evidence unless it came from the approved collector workflow on `main`. | Changed: bound artifact restoration to exact run provenance (`head_branch=main`, workflow path `.github/workflows/crossvenue-probe.yml`, successful scheduled/manual event), added regression tests for wrong-branch and wrong-workflow artifacts, and wired the explicit provenance contract into collector and health restoration. | Evidence: the previous selector accepted any successful scheduled/manual run that uploaded the shared artifact name, allowing a manual branch or different workflow to become the next evidence baseline. | Verification: artifact-selection provenance assertions and Python compilation passed locally; source, tests, both workflows, diary, and final GitHub writes were verified. | Unresolved: live Actions must confirm GitHub reports the expected workflow `path` field and restoration remains healthy. | Next: inspect the next collector and health runs for approved artifact selection, valid chain continuity, and non-stale health output.
- 2026-07-23T22:10:00Z | Thesis: last-known-good artifacts alone cannot reveal a stalled or repeatedly failing collector, so operational health must independently inspect live workflow execution. | Changed: added `crossvenue_actions_health.py`, restoration provenance retention, workflow-run freshness/stuck/repeated-failure gates, exact latest-success matching, five focused tests, health-workflow integration, and documentation without touching the frozen experiment contract or cutoff. | Evidence: the prior health workflow could report a valid old artifact while newer collector runs repeatedly failed or remained queued. | Verification: five focused tests and Python compilation passed locally; source, test, workflow, README, diary, and returned GitHub commits were verified. | Unresolved: the next hourly health run must prove the workflow-runs API response and restoration/latest-success comparison against live Actions. | Next: inspect `crossvenue_actions_health.json`, `crossvenue_restoration.json`, and `crossvenue_health.json`; if healthy, make no frozen-contract changes and continue accumulation.
